# FOUNDER_DECISIONS — 四項全部已裁決

> 2026-07-30 · Gate 8 · **本檔已結案**，保留為裁決紀錄
>
> | # | 決策 | Michael 的裁決 | 狀態 |
> |---|---|---|---|
> | 1 | push main 上 production | 推 | ✅ 已完成（origin/main = 1438f6d，prod 實查已上線） |
> | 2 | Stripe 四個 payment link | **先保留**，不建 | 程式待命，環境變數留空，付款鍵不渲染 |
> | 3 | 揭薪指數 15 筆抽查 | **直接上線，不寫經過稽核** | ✅ 已上線，verification 維持 `source_available_unverified` |
> | 4 | 首頁收斂 | **照建議做**：首屏一句話＋一顆按鈕，其餘降第二屏 | ✅ 已實作 |
>
> **不在這份清單上的**：能不能收費、EP、MOM、moonlighting、能不能提供一對一諮詢、
> 能不能公開價格。那些在 2026-07-29／30 已由你確認並正式 bypass，不會再問。

---

## 決策 1 · 要不要把 main 推上 production

**建議：推。**

理由：十一個 commit 全綠（typecheck、221 個測試、production build），付款鍵因為
環境變數未設而不會出現，所以推上去不會有人刷到卡。回滾是一行指令。

你的 machine hook 擋住機器 push main，所以只能你按：

```
git -C "C:\Users\michael\Desktop\ahamoment\ahamoment-green-talent-os-final\ahamoment-green-talent-os" push origin main
```

替代方案：先看 `claude/gate-8-autonomous-execution` 的 Vercel preview 再決定。

不決定的影響：production 維持舊版——11 檔服務、全額退費承諾、沒有導覽、
90 天自動刪除仍然只是文案上的承諾。

**回覆：** 「推」　或　「我先看 preview」

---

## 決策 2 · Stripe 四個 payment link 何時建

**建議：這週建。**

理由：程式與測試都完成了，缺的只有四個環境變數，加上 Stripe 後台的「付款後轉址」
設定。約 30 分鐘，逐步清單在 `docs/execution/STRIPE_SETUP.md`。

替代方案：先不建，維持「加 LINE → 通話 → 一對一確認 → 轉帳」。付款鍵不出現，
其餘一切照常運作。

不決定的影響：現金只能走轉帳。不影響任何其他功能，但線上買不了。

**回覆：** 「這週建」　或　「先不建」

---

## 決策 3 · 揭薪指數的 15 筆抽查誰做

**建議：你做，約 40 分鐘。**

做什麼：從三個已達門檻的格子各抽 5 筆，重開原始職缺頁核對薪資與資料庫是否一致，
然後把 `content/salaryIndex.ts` 裡的 verification 從 `source_available_unverified`
改成 `manually_verified`。

我沒有代做：沒複驗過就標成已核實，等於偽造驗證。

替代方案：維持 `source_available_unverified`。頁面照常上線，只是不能對外說
「經過稽核」。

不決定的影響：頁面可以上，但敘事上限被鎖住——不能宣稱「市場上沒有第二個人在做
這件事」，因為那句話需要資料經得起查。

**回覆：** 「我抽查」　或　「先不查照上」

---

## 決策 4 · 首頁要不要跟著收斂

**建議：改，但等你決定。**

現況不一致：首頁還有九個並列的一級出口，而服務與導覽已經收斂成三個決策入口。

改法：首屏收成一句話加一顆按鈕，其餘降到第二屏。

我沒有自己動的理由：首頁是你花最多心力寫的一頁，而判斷力旗艦卡是你 07-29
才親手放上去的。

替代方案：維持現狀。導覽有路網了，首頁只是比較擠。

不決定的影響：首頁與其他頁面的敘事不一致，新訪客仍要在九個出口裡自己選。

**回覆：** 「改，首屏留＿＿」　或　「不改」

---

# 沒有列進來但你可能會問的三件事

## 付款流程測過了嗎

單元測試層面測透了：簽章驗證、竄改 body、逾時重放、金鑰輪替、去重、
測試模式隔離、金額不進分析，共 16 個測試。

**真實刷卡沒測**，因為沒有 Stripe 憑證，而規則寫明不得在缺憑證時製造假的付款成功。
測試卡的逐步清單在 `STRIPE_SETUP.md` 第四節，含「按 Resend 重送一次確認去重生效」
那一步。

## purge 會不會誤刪

不會。它預設 dry run——`PURGE_ENABLED` 未設就只計數不寫入，而且 `?dry=1`
永遠強制預覽。要真的開始刪，設 `PURGE_ENABLED=true`。

建議先用 dry run 跑一次看它報幾筆，再決定要不要開。

## 舊的 offer id 會不會壞掉

不會。十個舊 offer 保留在 `OFFER_IDS` 裡，所以歷史 `scores` 列照樣解析；
它們只是永遠不會出現在任何公開頁面。

報告頁遇到 Gate 8 之前存下的舊分類，會自動退回現行的兩檔目錄，有測試守著。
