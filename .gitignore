node_modules/
.next/
.env
.env.local
.env*.local
*.log
.DS_Store

# Claude Code local settings — machine-specific allow-list, may hold secrets
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Generated files (tool-owned — do not track or hand-edit)
next-env.d.ts
tsconfig.tsbuildinfo

# local-only working files
scratch/
.mcp.json

# Real call records — contain a named person's own words about their career.
# The template is tracked; the records themselves never are.
ops/validation/records/
